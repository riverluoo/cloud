spring:
  cloud:
    config:
      name: user,feign,database,redis,key,rabbitmq
      label: master
      discovery:
        enabled: true
        serviceId: config


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/